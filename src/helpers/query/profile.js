module.exports = {
    editUser: 'UPDATE users SET ? WHERE id = ?',
    getData: 'SELECT * FROM users where id = ?',
    addAdress: 'INSERT INTO address SET ?',
    editAdress: 'UPDATE address SET ? WHERE id = ?',
}